"use client"

interface AiTutorSectionProps {
  lessonId: string
}

// This is a placeholder component that returns null
// It exists only to prevent import errors during the transition
// while we remove AI tutor functionality from the site
export function AiTutorSection({ lessonId }: AiTutorSectionProps) {
  return null
}
